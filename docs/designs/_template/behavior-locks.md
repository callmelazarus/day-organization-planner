# Behavior Locks: <name>

Invariants that must hold once this design is implemented. A behavior lock
is broken if any future change makes its proof condition false.

## Lock 1: <short description>

- **Invariant:** <the thing that must always be true>
- **Proof condition:** <how to check it — a test, a manual step>
- **Test pointer:** <path to the test that enforces this, if one exists>
